// This component is kept but skills are now shown via the SkillsSection component
// Keeping for compatibility with the experience page if used
export default function ToolsList() {
  return null;
}
